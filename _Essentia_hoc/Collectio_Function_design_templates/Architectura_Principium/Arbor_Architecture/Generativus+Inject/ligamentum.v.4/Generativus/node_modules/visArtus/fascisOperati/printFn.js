'use strict';

const { artus } = require('../index');

const communis = {
  fascisMethods: {
    methodA: () => undefined,
  },
  operatiMethods: {},
  closureForFn: {},
  thisForFn: {},
};
debugger
communis.fascisMethods.methodA = (closureForFn) => closureForFn;

communis.operatiMethods = {
  printFn() {
    console.log(this.fn);
    return this;
  },
};

// Use

// const fn = function(...args) {
//   console.dir({ args, this: this });
// };
//
// debugger
// const artusFn = artus(fn).printFn();
// artusFn(0);
// const r1 = artusFn.getOperati();
//
//
// debugger

const printFn = artus.bind(null, communis);

module.exports = printFn;

