'use strict';

const { artus } = require('v.1');

debugger
artus.fascis.returnBfn = (bfn, closureForFn) => {
  if (!closureForFn.flags.kill) {
    closureForFn.flags.kill = true;
    return bfn;
  } else return undefined;
};

const fn = function(...args) {
  console.dir({ args, this: this });
};

// Use
debugger
const artusFn = artus(fn);
// const r0 = artusFn.getOperati(); getOperati not exist!
artusFn('a');// call
const r1 = artusFn.getOperati();
// r1.closureForFn.flags.kill === true
artusFn('b');//a call is being dumped
const r2 = artusFn.getOperati(); // but r2.args = ["b"]


debugger
