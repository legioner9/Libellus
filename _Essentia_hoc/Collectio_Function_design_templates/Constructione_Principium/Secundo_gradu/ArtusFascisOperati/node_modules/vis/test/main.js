'use strict';

'use strict';

const { artus } = require('vis');

debugger
artus.fascis.fascisMethod = (closureForFn) => closureForFn;

const fn = function(...args) {
  console.dir({ args, this: this });
};

// Use
debugger
const artusFn = artus(fn).printFn()

artusFn('a');// call
const r1 = artusFn.getOperati();

// r1:
// args: ["a"]
// bfn: ƒ ()
// closureForFn: {}
// thisForFn: {}
