'use strict';

const { communis, artus } = require('vis');

communis.fascisMethod = (closureForFn) => closureForFn;

communis.operatiMethods = {
  printFn() {
    console.log(this.fn);
    return this;
  },
};


const fn = function(...args) {
  console.dir({ args, this: this });
};

// Use
debugger
const artusFn = artus(fn).printFn();
artusFn(0);
const r1 = artusFn.getOperati();


debugger
