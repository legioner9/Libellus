'use strict';

const { injectProtoFucus } = require('../index');
debugger

const resFn = injectProtoFucus((...args) => args);

resFn.addPrototype('toString2', function() {
  debugger
  return 'from toString2' + this.toString();
});
console.log(resFn.toString2()) ;
debugger
